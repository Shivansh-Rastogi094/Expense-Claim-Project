spring.application.name=expense-approval-backend
server.port=8080

# Database Connection Placeholders (Update these with your MySQL details)
spring.datasource.url=jdbc:mysql://localhost:3306/expense_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=Spring
spring.datasource.password=Rshivansh@094
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL Initialization Settings (for seeding data.sql)
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
